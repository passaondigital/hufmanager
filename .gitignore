# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Nie committen — auch wenn der aktuelle Inhalt nur unkritische VITE_-Variablen
# enthält, soll hier künftig kein Secret aus Versehen mitgecommittet werden.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI workspace artifacts (link state, version probes, etc.)
# Never commit these; they are local to whichever machine ran `supabase link`.
supabase/.temp/*
!supabase/.temp/.gitkeep
