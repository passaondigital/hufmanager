// Social Proof Widget removed — was showing potentially inaccurate numbers.
// Re-enable when real-time active user tracking is implemented.
export const SocialProofWidget = () => {
  return null;
};
